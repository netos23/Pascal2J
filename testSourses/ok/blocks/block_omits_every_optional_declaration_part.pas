{ ISO 7185, 6.2.1 Blocks.
  Each declaration part of a block may be empty, so the smallest legal
  program is a heading and a statement-part.  The statement-part itself
  may contain nothing but an empty-statement. }
program BlockOmitsEveryOptionalDeclarationPart;
begin
end.
